/*
	Exercise 1 - String calculator, part 1
  
	1.1. Calculator should have an add method: int add(str).
	1.2. For an empty string add() should return zero.
	1.3. For a single number (unsigned integer) add() should simply parse the string (e.g. "7" -> 7).
*/

describe('Calc', function () {
 
  describe('add()', function() {
  
    it('should exist', function () {
      
    });
    
    xdescribe('for an empty string', function () {
      it('should return zero', function () {
        
      });
    });
    
    xdescribe('for a single number', function () {
      it('should parse the input', function () {
        
      });
    });
    
  });
  
});